[project]
name = "data-reduction-routines"
version = "0.1.0"
description = "Experimental data reduction workflows"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.0,<3",
]

[tool.uv]
package = false
